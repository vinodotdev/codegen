use vino_provider::{Context, error::ProviderComponentError};

use crate::generated::{{snakeCase schema.unhyphenated}}::*;

pub(crate) async fn job(
  input: Inputs,
  output: Outputs,
  _context: Context<crate::State>,
) -> Result<(), Box<ProviderComponentError>> {
  Ok(())
}
