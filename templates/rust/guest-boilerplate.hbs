mod generated;
extern crate wapc_guest as guest;
pub use generated::*;
use guest::prelude::*;

#[no_mangle]
pub fn wapc_init() {
    Handlers::register_job(job);
}

fn job(input: Inputs, output: Outputs) -> HandlerResult<Signal> {
    Ok(Signal::Done)
}
