
{{#ifCond @root.type '===' 'wellknown'}}
pub use {{snakeCase interface.name}}::{{moduleName name}}::*;
{{else}}
pub use crate::components::generated::{{moduleName name}}::*;
{{/ifCond}}


{{#if multi}}
pub(crate) async fn job(
  _inputs: Vec<ComponentInputs>,
  _output: OutputPorts,
  _context: crate::Context,
) -> JobResult {
  unimplemented!();
}
{{else}}
pub(crate) async fn job(
  input: Inputs,
  output: OutputPorts,
  context: crate::Context,
) -> JobResult {
  Ok(())
}
{{/if}}